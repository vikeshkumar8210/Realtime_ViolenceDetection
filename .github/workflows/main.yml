name: Python application

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKER }}" | docker login -u "vikesh82" --password-stdin "Vikesh@8210"
        docker image build -t vikeshkumar8210/Realtime_ViolenceDetection:v1 .
        docker push vikeshkumar8210/Realtime_ViolenceDetection:v1
